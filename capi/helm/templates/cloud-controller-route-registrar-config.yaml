{{ if .Values.cloudController.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cloud-controller-route-registrar-config
data:
  route-registrar.json: |-
    {
      "message_bus_servers": [
        {
          "host": "{{ tpl .Values.cloudController.nats.address . }}"
        }
      ],
      "nats_mtls_config": {
        "enabled": true,
        "cert_path": "/etc/ssl/certs/cloud-controller/tls.crt",
        "key_path": "/etc/ssl/certs/cloud-controller/tls.key",
        "ca_path": "/etc/ssl/certs/cloud-controller/ca.crt"
      },
      "host": "",
      "routes": [
        {
          "name": "api",
          "tls_port": 9024,
          "registration_interval": "10s",
          "server_cert_domain_san": "api.{{ .Values.cloudController.systemDomain }}",
          "tags": {
            "component": "CloudController"
          },
          "uris": [
            "api.{{ .Values.cloudController.systemDomain }}"
          ]
        }
      ]
    }
{{ end }}