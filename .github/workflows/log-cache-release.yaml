name: Release log-cache

on:
  push:
    branches:
      - main
    paths:
      - 'log-cache/helm/Chart.yaml'
      
env:
  REGISTRY_PREFIX: ghcr.io/cloudfoundry/k8s/
  HELM_REGISTRY_PREFIX: ghcr.io/cloudfoundry/helm/


jobs:
  release:
    runs-on: [self-hosted, solinas]
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v6
      - name: Determine Versions
        working-directory: log-cache
        run: |
          echo "VERSION=$(cat helm/Chart.yaml | yq e '.version')" >> GITHUB_ENV
      - name: Release Images
        working-directory: log-cache
        run: |
          # Check if image available
          if docker pull $REGISTRY_PREFIX/log-cache:$VERSION > /dev/null 2>&1; then
             echo "log-cache image already exists in version $VERSION."
          else
            docker buildx bake #--push
          fi
      - name: Publish Helm Chart
        working-directory: log-cache
        run: |
          # Check if chart available
          if helm show chart oci://$HELM_REGISTRY_PREFIX/log-cache --version $VERSION > /dev/null 2>&1; then
             echo "Helm chart already exists in version $VERSION."
          else
            helm package ./log-cache/helm -d ${{ runner.temp }}
            # helm push ${{ runner.temp }}/log-cache-*.tgz oci://$HELM_REGISTRY_PREFIX/
          fi
