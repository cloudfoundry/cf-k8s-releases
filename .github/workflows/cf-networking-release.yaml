name: Release cf-networking

on:
  push:
    branches:
      - main
    paths:
      - 'cf-networking/helm/Chart.yaml'
      
env:
  REGISTRY_PREFIX: ghcr.io/cloudfoundry/k8s/
  HELM_REGISTRY_PREFIX: ghcr.io/cloudfoundry/helm/


jobs:
  cf-networking-release:
    runs-on: [self-hosted, solinas]
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v6
      - name: Determine Versions
        working-directory: cf-networking
        run: |
          echo "VERSION=$(cat helm/Chart.yaml | yq e '.version')" >> GITHUB_ENV
      - name: Release Images
        working-directory: cf-networking
        run: |
          # Check if image available
          if docker pull $REGISTRY_PREFIX/policy-server:$VERSION > /dev/null 2>&1; then
             echo "policy-server image already exists in version $VERSION."
          else
            docker buildx bake #--push
          fi
      - name: Publish Helm Chart
        working-directory: cf-networking
        run: |
          # Check if chart available
          if helm show chart oci://$HELM_REGISTRY_PREFIX/cf-networking --version $VERSION > /dev/null 2>&1; then
             echo "Helm chart already exists in version $VERSION."
          else
            helm package ./helm -d ${{ runner.temp }}
            # helm push ${{ runner.temp }}/cf-networking-*.tgz oci://$HELM_REGISTRY_PREFIX/
          fi
